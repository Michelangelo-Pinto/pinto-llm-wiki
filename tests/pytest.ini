[pytest]
markers =
    unit: no containers required
    integration: needs qdrant-db
    integration_stack: needs full docker compose stack
    e2e: ingestion pipeline end-to-end
    performance: benchmarks (slow)
    regression: wiki mcp tool regression
    smoke: SSE transport smoke tests
